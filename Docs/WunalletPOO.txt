Usuario

+ arrayList listaDeInscritos: Objetos tipo Cuenta los cuales contengan la cc, nmro de cuenta, tipo, banco

Métodos:

Inscribir( int cc, int nroCuenta, String tipoDeCuenta, String nombreBanco){
    - Se busca de forma iterativa usando el método de clase 'extraerBanco(nombreBanco)' si el banco existe, en cuyo caso retornará todo el objeto tipo Banco. De lo contrario, retornará Null para interpretar un error de input en el nombreBanco (no se encuentra en la lista).

    bancoDestino = EntidadBancaria.extraerBanco(nombreBanco)

    - Si el Banco existe, ingrese a la lista de cuentas de dicho Banco y busque que coincidan los demás parámetros en las cuentas. Si hay alguna cuenta retornela o si no, retorne Null.

    cuentaDestino = bancoDestino.extraerCuenta(int cc, int nroCuenta, String tipoDeCuenta)

    - Si la cuenta existe y fue retornada, añádala a la listaDeInscritos del Usuario

    this.listaInscritos.add(cuentaDestino)
    
------------------------------------
Banco

+ static arrayList listaDeBancos: Objetos de tipo Banco. Todos los objetos que se creen de Banco se deben añadir a la lista (añadir al constructor).

+ String nombreBanco: El nombre del banco.

+ ArrayList listaDeCuentas: Listas de objetos de tipo cuenta que tiene asociadas un Banco.

Métodos

+ extraerCuenta(int cc, int nroCuenta, String tipoDeCuenta) {
    
    - De forma iterativa se recorre la listaDeCuentas del Banco, buscando que concuerden los 3 parámetros con los atributos de alguna Cuenta. De ser así retorna el objeto completo (de tipo Cuenta), o en caso contrario, retorna Null.
    

------------------------------------


